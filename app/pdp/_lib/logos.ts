/**
 * Reserved for brand logos that need to be stored locally. Most brands
 * reference their real logo directly by CDN URL in chrome.tsx. East Coast
 * serves its logo only as an inline data URI on the live site (no stable
 * URL), so its header uses a styled wordmark in our type system instead.
 */
export {};
