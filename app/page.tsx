// The Watsco Design Templates index is the home page.
// It lives in `app/pdp/page.tsx`; root re-exports it so `/` and `/pdp`
// both render it and every existing `/pdp/...` link stays active.
export { default, metadata } from "./pdp/_index"
